name: Test Action

on:
  push:
    branches:
      - main  # or any branch you want to trigger the action

jobs:
  test:
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: 22f3001834@ds.study.iitm.ac.in
        run: echo "Hello, world!"
